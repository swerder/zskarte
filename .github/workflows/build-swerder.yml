on:
  push:
    branches:
      - swerder

env:
  REGISTRY: ghcr.io
  IMAGE_NAME: ghcr.io/${{ github.repository }}
  TAG_NAME: ${{ github.sha }}

jobs:
  build-and-deploy-server:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
    steps:
      - uses: actions/checkout@master

      - name: Login to GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and Push Docker Image to GitHub
        id: build-image
        run: |
          docker build "$GITHUB_WORKSPACE/" -f  "Dockerfile" -t ${{ env.IMAGE_NAME }}-server:${{ env.TAG_NAME }} -t ${{ env.IMAGE_NAME }}-server:latest --label dockerfile-path=Dockerfile
          docker push ${{ env.IMAGE_NAME }}-server:${{ env.TAG_NAME }}
          docker push ${{ env.IMAGE_NAME }}-server:latest
  build-app:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
    steps:
      - uses: actions/checkout@master

      - name: Login to GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and Push Docker Image to GitHub
        id: build-image
        run: |
          docker build "$GITHUB_WORKSPACE/" -f "DockerfileApp" -t ${{ env.IMAGE_NAME }}-client:${{ env.TAG_NAME }} -t ${{ env.IMAGE_NAME }}-client:latest --label dockerfile-path=DockerfileApp
          docker push ${{ env.IMAGE_NAME }}-client:${{ env.TAG_NAME }}
          docker push ${{ env.IMAGE_NAME }}-client:latest
          docker build "$GITHUB_WORKSPACE/" -f "DockerfileApp" --build-arg="CONFIG=swerder-debug" -t ${{ env.IMAGE_NAME }}-client:${{ env.TAG_NAME }}-debug -t ${{ env.IMAGE_NAME }}-client:latest-debug --label dockerfile-path=DockerfileApp
          docker push ${{ env.IMAGE_NAME }}-client:${{ env.TAG_NAME }}-debug
          docker push ${{ env.IMAGE_NAME }}-client:latest-debug
